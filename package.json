{
    "scripts": {
        "build:css": "postcss css/styles.css -o css/styles.min.css",
        "build:js": "esbuild js/main.js --bundle --minify --outfile=js/main.min.js",
        "build": "npm run build:css && npm run build:js"
    },
    "dependencies": {
        "marked": "^4.0.0",
        "micromodal": "^0.4.6",
        "swiper": "^8.0.0"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.0",
        "cssnano": "^5.0.0",
        "esbuild": "^0.14.0",
        "postcss": "^8.4.0",
        "postcss-cli": "^9.0.0"
    }
} 