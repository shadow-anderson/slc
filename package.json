{
    "scripts": {
  "build:css": "node build-css.js",
  "build:js": "npx esbuild js/main.js --bundle --minify --outfile=main.min.js",
  "build": "npm run build:css && npm run build:js"
},
    "dependencies": {
        "marked": "^4.0.0",
        "micromodal": "^0.4.6",
        "swiper": "^8.0.0"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.20",
        "cssnano": "^5.1.15",
        "esbuild": "^0.14.54",
        "postcss": "^8.4.0",
        "postcss-cli": "^9.1.0"
    }
}
